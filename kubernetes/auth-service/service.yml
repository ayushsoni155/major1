apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: rapidbase
  labels:
    app: auth-service
spec:
  selector:
    app: auth-service
  ports:
    - name: http
      port: 4001
      targetPort: 4001
      protocol: TCP
  type: ClusterIP
