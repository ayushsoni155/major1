apiVersion: v1
kind: Service
metadata:
  name: nginx-gateway
  namespace: rapidbase
  labels:
    app: nginx-gateway
spec:
  selector:
    app: nginx-gateway
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
